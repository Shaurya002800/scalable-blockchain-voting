# Shared cryptography

This package will hold browser-safe encryption, nullifier, Merkle-tree, and
proof serialization utilities shared by the frontend and Node.js scripts.

Cryptographic choices and serialization formats must be documented before
implementation so the circuits, TypeScript code, and Solidity verifiers agree.
