# Frontend

The Next.js frontend is deferred until the registration and ballot-package
contracts are stable. Planned routes are `/register`, `/vote`, `/batcher`,
`/tally`, `/verify`, and `/dashboard`.
